version: "3.8"

services:
  web:
    build: .
    container_name: horahore-landing-dev
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NGINX_HOST=${DOMAIN:-dev.localhost}
      - NGINX_PORT=80
      - ENVIRONMENT=development
    networks:
      - web-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./logs:/var/log/nginx

  nginx-proxy:
    image: nginx:alpine
    container_name: nginx-proxy-dev
    restart: unless-stopped
    ports:
      - "8000:80"
      - "8443:443"
    volumes:
      - ./nginx-proxy.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl
      - ./proxy-logs:/var/log/nginx
    depends_on:
      - web
    networks:
      - web-network

networks:
  web-network:
    driver: bridge
